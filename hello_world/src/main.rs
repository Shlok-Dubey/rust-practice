// to initialsed the project we should use "cargo init" cmd this will make "cargo.toml" and "main.rs" files
/* to initialsed library use "cargo init --lib", you will be able to use this in diffrent projects
 but won't be able to excute it */ 
// to run "cargo run"
// to build "cargo build" --> this will create target/debug/file_name (this is development build)
/* to production build "cargo build --release" --> this will create target/release/file_name (more
 optimised then development) */


fn main() {
    println!("Hello, world!");
}
